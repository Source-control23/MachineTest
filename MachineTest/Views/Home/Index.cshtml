@{
    ViewData["Title"] = "Shop4U";
    var cartItems = ViewData["CartItems"] as List<Items>;
    var total = Convert.ToInt32(ViewData["Total"]);

}
<div class="card">
    <div class="card-header text-center  bg-black">
        <b class="text-warning h2 ">Shop 4 U</b>
    </div>
    <div class="card-body">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Item</th>
                    <th>Qty</th>
                    <th>Price/Item</th>
                    <th></th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in cartItems)
                {
                    <tr>
                        <td>@item.ItemName</td>
                        <td>@item.Quantity</td>
                        <td>@item.ItemPrice</td>
                        <td></td>
                        <td></td>
                    </tr>
                }
                <tr>
                    <td>Total</td>
                    <td></td>
                    <td><input readonly value="@total" /></td>
                    <td><input type="text" placeholder="cupon code" /></td>
                    <td><button>Calculate bill</button></td>
                </tr>
            </tbody>

        </table>
    </div>
</div>